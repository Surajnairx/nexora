/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  devIndicators: false,
  images: {
    domains: ["images.pexels.com"],
  },
};

export default nextConfig;
